# BookwormPRO 修复进度 (会话 [协助BWMPRO调试])

## 用户偏好
- **2026-04-26**：以后修复类任务**直接执行**，不再询问/给方案让用户选

## 已交付（10 个 commit，全部已 push 到 huakoh/BookwormPRO）

| commit | 内容 |
|---|---|
| `0ad8463e4` | skill 目录 hermes-agent → bookwormpro 改名 |
| `9578494cb` | docker memory 种子 + entrypoint bootstrap |
| `89f5568bb` | docker-compose host bridge 卷挂载 |
| `cdaf1ab0a` | setup-host-bridge.{sh,ps1} + docs |
| `4d7798285` | system prompt: HOST_BRIDGE_ENVIRONMENT_HINT |
| `228079c74` | system prompt: NATIVE_HOST_ENVIRONMENT_HINT (是修好 Windows 原生模式的关键) |
| `e48986dfb` | refusal_interceptor + banner 能力声明行 |
| `8c6a3d662` | prompt 缓存自愈（mtime 比对自动失效） |
| `2bcf693f7` | doctor 三新检查（runtime FS / memory / cache） |
| `f84bad76f` | /cost 命令（session/today/month 三段汇总） |
| `fb16e1fd0` | setup --quick 入口 |

## 关键发现（不要忘）
1. 用户的 BookwormPRO 是 **pip editable install** 在 system Python 3.12，不是 Docker
2. `D:\repos\hermes-agent` 即 live 代码，git pull 不需要 redeploy
3. 重启流程：`Stop-Process bookworm` → 删 `.skills_prompt_snapshot.json` → `bookworm`
4. 中转站：`bww.letcareme.com/v1`（OpenAI 兼容，provider=openrouter）

## 未完成 / 待办

### deepseek-v4-pro thinking-mode bug（真代码缺陷）✅ 2026-04-26 修复
- 现象：`content[].thinking must be passed back to the API` / `reasoning_content in thinking mode must be passed back`
- **根因**：`run_agent.py:_build_assistant_message` 的 elif 分支检查 `msg.get("tool_calls")`，但 tool_calls 在 line 7732 才被赋值 → elif 永远不触发 → DeepSeek/Kimi tool-call 消息被以无 reasoning_content 字段持久化 → 重放时 HTTP 400
- **修复**：改为检查 `assistant_message.tool_calls`（已存在的 OpenAI 响应对象），同时把 Kimi 也纳入同一 pin 逻辑（之前 Kimi 只在 replay 路径有补丁，创建路径漏了）
- **测试**：tests/run_agent/test_deepseek_reasoning_content_echo.py +5 个 TestBuildAssistantMessageDeepSeekPin 用例，全部 26/26 ✓

### credential_pool 双 openrouter 凭据治理 ✅ 2026-04-26
- **修复**：`_seed_from_env` 检测 manual 条目的非默认 base_url，存在则跳过 env 种子
- **诊断**：canary 也加了 base_url 冲突 WARN（多个不同 base_url 触发警告）
- 测试：tests/agent/test_credential_pool.py +3 用例

### memory introspection ✅ 2026-04-26
- 新增 `bookworm memory show [memory|user]`：列出 entries + bytes
- 新增 `bookworm memory why <substring>`：定位条目源 + 路径
- 测试：tests/bwm_cli/test_memory_introspection.py 13/13

### post-deploy canary 自动化 ✅ 2026-04-26
- 新增 `bookworm canary [--live]`：5 项快速 smoke (config / imports / pool / cache / memory)
- setup tail 自动运行 canary（仅在 FAIL/WARN 时打印；BOOKWORMPRO_SKIP_POST_DEPLOY_CANARY 可关闭）
- 测试：tests/bwm_cli/test_canary.py 16/16

### 用户体验下一批 (剩余 P2)
- 凭据池 schema 校验（更深的字段一致性，目前只验证 base_url）

## 测试覆盖（本会话新增 40 测试，全过）
- `tests/test_bwm_constants.py` 19 ✓ (含 is_host_bridge_active / is_native_install)
- `tests/agent/test_prompt_builder.py::TestEnvironmentHints` 8 ✓
- `tests/agent/test_prompt_builder.py::TestSkillsPromptSnapshotSelfHeal` 5 ✓
- `tests/agent/test_refusal_interceptor.py` 16 ✓
- `tests/bwm_cli/test_doctor_extensions.py` 8 ✓
- `tests/bwm_cli/test_cost_command.py` 7 ✓
- `tests/bwm_cli/test_setup_quick_flag.py` 4 ✓
